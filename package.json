{
  "name": "site-energia-solar",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd site && npm run dev",
    "build": "cd site && npm run build",
    "start": "cd site && npm start",
    "lint": "cd site && npm run lint"
  }
}
